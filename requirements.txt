# Phase 1 (data generation) — lightweight, no GPU/ML deps.
numpy>=1.24
scipy>=1.10
h5py>=3.8
matplotlib>=3.7
tqdm>=4.65
pydantic>=2.0
pyyaml>=6.0.3

# Surrogate training / W&B / optional TFNO — install from repo root:
#   pip install -r requirements-train.txt
# Optional DeepXDE PINN experiments are not pinned here.

# Dev / test.
pytest>=7.4
